#include <stdio.h>
#include <string.h>

main(){
    int vetor[10];
    char numeros[30];
    int i;

    puts("digite 10 numeros separados por virgula");
    scanf("%s", numeros);

    for(i = 0;i < 30)

    return 0;
}